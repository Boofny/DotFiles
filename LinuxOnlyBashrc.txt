#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias dysk='dysk -a'
alias ls='lsd'
alias lsa='lsd -a'
alias lst='lsd --tree'
alias lsl='lsd -l'
alias c='clear'
alias cls='clear'
alias ..='cd ..'
alias neo='fastfetch'
alias grep='grep --color=auto'
alias postStatus='sudo systemctl status postgresql'
alias postRoot='sudo -u postgres pgcli'
alias postStart='sudo systemctl start postgresql'
alias postStop='sudo systemctl stop postgresql'
alias postUser='pgcli -U myuser -d mydb2 -h localhost'

alias dockStart='sudo systemctl start docker.service'
alias dockStop='sudo systemctl stop docker.service'
alias dockStat='sudo systemctl status docker.service'

alias esp='source ~/commands/espStruct.sh'
alias fg='bash ~/gitThigns/DotFiles/scripts/fuzzy.sh'
alias cdf='source ~/gitThigns/DotFiles/scripts/cdFuzz.sh'
alias vim='bash ~/gitThigns/DotFiles/scripts/NvimScript.sh'

alias menu='xfce4-terminal --show-menubar'
PS1='[\u@\h \W]\$ '
eval "$(starship init bash)"
export EDITOR=nvim

function y() {
	local tmp="$(mktemp -t "yazi-cwd.XXXXXX")" cwd
	yazi "$@" --cwd-file="$tmp"
	IFS= read -r -d '' cwd < "$tmp"
	[ -n "$cwd" ] && [ "$cwd" != "$PWD" ] && builtin cd -- "$cwd"
	rm -f -- "$tmp"
}
GOPATH=$HOME/go  PATH=$PATH:/usr/local/go/bin:$GOPATH/bin

# Created by `pipx` on 2025-11-15 07:42:03
export PATH="$PATH:/home/brandon/.local/bin"
export MAINWORKDIR="/home/brandon/gitThigns/"

export BAT_THEME="Catppuccin Mocha"

alias fzf="fzf --style full --preview 'bat --color=always --style=numbers --line-range=:500 {}' --margin=10%,10% --layout reverse --border --border-label 'Find'"
